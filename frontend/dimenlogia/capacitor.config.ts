import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.marcsoft.dimenlogia',
  appName: 'dimenlogia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
